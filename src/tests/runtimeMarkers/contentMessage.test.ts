import { expect, test } from 'vitest';
import * as mod from '../../roborockCommunication/models/contentMessage.js';

test('contentMessage runtime marker present', () => {
  expect(mod).toBeDefined();
});
