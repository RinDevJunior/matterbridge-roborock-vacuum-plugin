import { expect, test } from 'vitest';
import * as mod from '../../roborockCommunication/Zmodel/batteryMessage.js';

test('batteryMessage runtime marker present', () => {
  expect(mod).toBeDefined();
});
