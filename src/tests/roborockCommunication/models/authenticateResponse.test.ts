import { expect, it } from 'vitest';

import * as mod from '../../../../src/roborockCommunication/models/authenticateResponse.js';

it('imports authenticateResponse without runtime error', () => {
	expect(mod).toBeTruthy();
});
