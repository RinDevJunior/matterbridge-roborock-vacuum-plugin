/**
 * Default delay for unregistering devices on shutdown (milliseconds).
 */
export const UNREGISTER_DEVICES_DELAY_MS = 500;

/**
 * `DeviceModel.Q10_S5_PLUS` short code, used to route Q10-specific map/trace parsing and the `currentArea` write guard.
 */
export const Q10_MODEL_SHORT_CODE = 'ss07';
