/**
 * Distance-related constants for cleaning modes.
 * @module constants/distance
 */

/**
 * Distance offset when "off base" mode is disabled (0).
 */
export const DISTANCE_OFF_BASE = 0;

/**
 * Multiplier for distance offset calculation (150).
 * Used to convert distance_off parameter to actual distance.
 */
export const DISTANCE_MULTIPLIER = 150;

/**
 * Default distance offset value (300).
 */
export const DISTANCE_DEFAULT = 300;
