# Persistent task proposals

Plans in this folder are **not** deleted by `clean-paths.mjs` during task wrap-up.

Use for deferred work (e.g. Phase 2) that a future architect or implementer should read without relying on ephemeral `workspace/<task>/` folders.
