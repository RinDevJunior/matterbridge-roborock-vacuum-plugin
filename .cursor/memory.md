# Project Shared Memory

**Not used.** Cursor reads and writes [`.claude/memory.md`](../.claude/memory.md) directly — auto-loaded via `AGENTS.md` (`@.claude/memory.md`).

This file exists for structural parity with `.claude/memory.md` only. Do not read this stub.
